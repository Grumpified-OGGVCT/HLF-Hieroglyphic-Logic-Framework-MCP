{
    "scan_interval_sec": 30.0,
    "alert_threshold": 3,
    "targets": [
        {
            "target_id": "hlf-mcp-server",
            "process_name": "python",
            "check_interval_sec": 15.0,
            "max_restarts": 3,
            "health_check_url": "http://hlf-mcp:${HLF_PORT:-8000}/health",
            "resource_limits": {"max_memory_mb": 512, "max_cpu_percent": 80}
        }
    ]
}

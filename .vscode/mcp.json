{
  "servers": {
    "hlf-mcp": {
      "type": "stdio",
      "command": "uv",
      "args": ["run", "hlf-mcp"],
      "cwd": "${workspaceFolder}",
      "description": "HLF packaged MCP server (stdio dogfooding entrypoint)",
      "icon": "🪶"
    }
  }
}
